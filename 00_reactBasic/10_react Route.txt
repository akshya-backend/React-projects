React Router is the official library used for routing in React applications. It allows you to navigate between pages (or components) without reloading the entire app.

Here’s a quick breakdown:

🔹 Install React Router
npm install react-router-dom

🔹 Basic Example
import { BrowserRouter, Routes, Route, Link } from "react-router-dom";

function Home() {
  return <h2>Home Page</h2>;
}

function About() {
  return <h2>About Page</h2>;
}

function App() {
  return (
    <BrowserRouter>
      <nav>
        <Link to="/">Home</Link> |{" "}
        <Link to="/about">About</Link>
      </nav>

      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;

🔹 Key Parts

BrowserRouter → Wrapper that enables routing.

Routes → Container for all route definitions.

Route → Defines a path (/, /about) and the component to render.

Link → Used instead of <a> to navigate without reloading.